<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MySpinner" parent="Widget.AppCompat.DropDownItem.Spinner">
        <item name="arrowTint">@android:color/white</item>
        <item name="textTint">@android:color/white</item>
        <item name="backgroundTint">@color/colorAccent</item>
        <item name="backgroundSelector">@color/colorAccent</item>
    </style>

</resources>
